# hadolint configuration
# https://github.com/hadolint/hadolint#configure
ignored:
  - DL3018  # Pin versions in apk add (alpine packages change frequently, acceptable risk)
  - DL3059  # Multiple consecutive RUN instructions (clarity over layer optimization in dev)
